# 设计模式学习笔记

[TOC]



## 一、观察者模式

### 1. 什么是观察者模式

观察者模式解决的问题是：

> 一个对象发生变化后，如何通知多个关注它的对象。

例如无人机地面站中，无人机的位置发生变化：

```text
无人机位置发生变化
        ↓
      通知
        ↓
 ┌──────┼──────┐
 ↓      ↓      ↓
地图   状态面板  告警模块
```

无人机不需要关心：

- 谁在关注它
- 关注者具体怎么处理
- 有多少个关注者

它只负责：

> 状态发生变化 → 发出通知。

------

### 2. 传统观察者模式

最经典的实现方式是：

> 被观察者内部保存观察者对象，当状态发生变化时，主动调用所有观察者的通知函数。

结构：

```text
             Subject
          （被观察者）
               │
        保存 Observer
               │
     ┌─────────┼─────────┐
     ↓         ↓         ↓
 Observer   Observer   Observer
```

例如：

```cpp
class Observer
{
public:
    virtual void update() = 0;
    virtual ~Observer() = default;
};
```

被观察者：

```cpp
class Subject
{
private:
    std::vector<Observer*> observers;

public:
    void attach(Observer* observer)
    {
        observers.push_back(observer);
    }

    void notify()
    {
        for (auto observer : observers)
        {
            observer->update();
        }
    }
};
```

使用：

```cpp
Drone drone;

MapView map;
StatusView status;

drone.attach(&map);
drone.attach(&status);

drone.notify();
```

这里的关系是：

```text
Observer
    │
    │ attach()
    ↓
Subject
    │
    │ notify()
    ↓
Observer::update()
```

所以传统观察者模式的一个重要特点是：

> **观察者主动告诉 Subject：“我要观察你。”**
>
> **Subject 保存这些 Observer，并负责通知它们。**

------

### 3. 回调函数实现

观察者模式不一定非要定义一个 `Observer` 基类。

也可以直接保存回调函数。

例如：

```cpp
class Drone
{
private:
    std::vector<std::function<void()>> callbacks;

public:
    void subscribe(std::function<void()> callback)
    {
        callbacks.push_back(callback);
    }

    void notify()
    {
        for (auto& callback : callbacks)
        {
            callback();
        }
    }
};
```

使用：

```cpp
Drone drone;

drone.subscribe([]()
{
    std::cout << "地图更新\n";
});

drone.subscribe([]()
{
    std::cout << "状态面板更新\n";
});

drone.notify();
```

结构：

```text
Drone
  │
  │ 保存回调函数
  ↓
┌────────┬────────┐
↓        ↓        ↓
Callback Callback Callback
```

这种方式和传统观察者模式的思想基本一样：

> 一个对象发生变化 → 执行所有注册的处理函数。

区别只是：

传统方式：

```text
Observer对象
    ↓
update()
```

回调方式：

```text
函数
    ↓
直接执行
```

回调方式通常更加轻量。

------

### 4. Qt Signal / Slot

Qt 中的 Signal / Slot 机制也可以实现类似观察者模式的功能。

例如无人机：

```cpp
class Drone : public QObject
{
    Q_OBJECT

signals:
    void positionChanged(double lon, double lat);
};
```

地图：

```cpp
class MapView : public QObject
{
    Q_OBJECT

public slots:
    void updatePosition(double lon, double lat)
    {
        // 更新地图
    }
};
```

连接：

```cpp
connect(
    &drone,
    &Drone::positionChanged,
    &mapView,
    &MapView::updatePosition
);
```

结构：

```text
Drone
  │
  │ Signal
  ↓
positionChanged()
  │
  ↓
Qt Signal/Slot 机制
  │
  ↓
MapView::updatePosition()
```

一个 Signal 也可以连接多个 Slot：

```text
             Drone
               │
       positionChanged()
               │
       ┌───────┼───────┐
       ↓       ↓       ↓
     MapView  Status   Alarm
```

所以可以把 Qt Signal / Slot 理解为：

> **Qt 提供的一套成熟的事件通知机制，可以实现观察者模式类似的效果。**

相比自己实现观察者，Qt Signal / Slot 还提供了很多额外能力，例如：

- 一个信号连接多个槽
- 自动管理连接关系
- 支持跨线程通信
- 支持异步队列连接
- 可以传递参数

------

### 5. 消息队列 / 发布订阅

再进一步，如果不希望 Subject 和 Observer 直接认识彼此，可以增加一个中间的消息系统。

传统观察者：

```text
Subject
   │
   ↓
Observer
```

消息队列：

```text
Publisher
    │
    ↓
消息队列 / Broker
    │
    ├────────┬────────┐
    ↓        ↓        ↓
 Consumer  Consumer  Consumer
```

例如无人机发布位置：

```text
Drone
  │
  ↓
Publisher
  │
  ↓
DronePosition
  │
  ↓
消息系统
  │
  ├───────┬───────┐
  ↓       ↓       ↓
地图     状态     告警
```

这里 Drone 不再保存：

```text
MapView
StatusView
Alarm
```

而只负责发布：

```text
DronePosition
```

谁关心这个消息，谁自己订阅。

所以：

> **消息队列进一步解耦了消息生产者和消费者。**

------

### 6. DDS 的实现方式

你之前使用 DDS 时，就是典型的：

> **发布 / 订阅模型。**

基本结构：

```text
Publisher
    │
    │ 发布 Topic
    ↓
   DDS
    │
    │ Topic
    ↓
┌───┴────┬────────┐
↓        ↓        ↓
Subscriber Subscriber Subscriber
   │        │        │
   ↓        ↓        ↓
Callback  Callback  Callback
```

例如无人机发布位置：

```text
Drone
  ↓
Publisher
  ↓
Topic: DronePosition
  ↓
DDS Middleware
  ↓
┌──────────┬──────────┬──────────┐
↓          ↓          ↓
地图       状态       告警
Subscriber Subscriber Subscriber
```

Subscriber 可以订阅 Topic，并绑定处理函数。

概念上类似：

```cpp
subscriber.subscribe(
    "DronePosition",
    [](const DronePosition& data)
    {
        // 处理无人机位置
    }
);
```

所以：

> **DDS 中确实是订阅者主动订阅 Topic，然后绑定处理函数。**

------

### 7. 为什么 DDS 和传统观察者模式看起来是反过来的？

这是之前讨论中最容易产生疑问的地方。

传统观察者模式：

```text
Observer
    │
    │ attach()
    ↓
Subject
```

Subject 内部保存：

```cpp
std::vector<Observer*> observers;
```

因此：

> Observer 告诉 Subject：“我关注你。”

然后 Subject 保存 Observer。

而 DDS：

```text
Subscriber
    │
    │ subscribe("DronePosition")
    ↓
DDS Middleware
```

DDS 中不是 Drone 保存：

```text
MapSubscriber
StatusSubscriber
AlarmSubscriber
```

而是 DDS 中间件管理 Topic 和 Subscriber 之间的关系。

可以简单理解成：

```text
传统观察者：

Subject 自己管理观察者


DDS：

DDS Middleware 帮你管理订阅者
```

所以两者并不是谁对谁错，而是：

> **观察者关系被放在了不同的位置。**

传统观察者：

```text
Subject
   ↓
Observer列表
```

DDS：

```text
Publisher
   ↓
DDS Middleware
   ↓
Topic
   ↓
Subscriber列表
```

------

### 8. 几种实现方式的对比

| 实现方式       | 核心关系                       | 谁管理订阅关系 | 典型使用场景       |
| -------------- | ------------------------------ | -------------- | ------------------ |
| 传统观察者     | Subject → Observer             | Subject        | 普通 C++ 对象      |
| 回调函数       | Subject → Callback             | Subject        | 简单事件通知       |
| Qt Signal/Slot | Signal → Slot                  | Qt             | Qt 应用程序        |
| 消息队列       | Publisher → Message → Consumer | 消息系统       | 进程间、分布式系统 |
| DDS            | Publisher → Topic → Subscriber | DDS Middleware | 实时分布式系统     |

------

### 9. 它们之间的关系

虽然实现方式不同，但是解决的问题非常类似：

```text
一个事件 / 状态发生变化
            ↓
          通知
            ↓
     所有感兴趣的对象
```

只是通知机制不断变化：

```text
传统观察者
    ↓
Subject 直接通知 Observer


回调函数
    ↓
Subject 直接执行 Callback


Qt Signal/Slot
    ↓
Qt 负责事件连接和通知


消息队列
    ↓
消息系统负责转发消息


DDS
    ↓
DDS Middleware 根据 Topic 完成发布/订阅
```

所以可以这样理解：

> **观察者模式是一种设计思想，而传统 Observer、Callback、Qt Signal/Slot、消息队列、DDS 都可以实现“一个变化通知多个关注者”这一思想。**

但它们的层次并不完全一样。

传统观察者主要解决：

> **对象之间的通知问题。**

DDS 主要解决：

> **分布式系统中的数据发布、订阅和通信问题。**

------

### 10. 最终快速记忆

#### 传统观察者

```text
Subject
   ↓
Observer
```

> Subject 保存 Observer，状态变化后主动通知。

------

#### 回调

```text
Subject
   ↓
Callback
```

> 不需要 Observer 类，直接注册处理函数。

------

#### Qt Signal / Slot

```text
Signal
   ↓
Slot
```

> Qt 提供的事件通知机制。

------

#### 消息队列

```text
Publisher
   ↓
Message Queue
   ↓
Consumer
```

> 通过消息系统进一步解耦生产者和消费者。

------

#### DDS

```text
Publisher
   ↓
Topic
   ↓
DDS Middleware
   ↓
Subscriber
   ↓
Callback
```

> 基于 Topic 的分布式发布 / 订阅系统。

------

### 11. 一句话总结

> **观察者模式解决的是“一个对象发生变化后，如何通知所有关注它的对象”。**
>
> **传统观察者是直接通知，回调是函数通知，Qt Signal/Slot 是 Qt 机制通知，消息队列和 DDS 则通过中间消息系统进行发布 / 订阅。**
>
> **它们解决的问题相似，但所处的层次不同：观察者模式主要解决对象之间的通知与解耦，而 DDS 主要解决分布式系统中的数据发布、订阅与通信。**

## 二、工厂模式

### 1. 工厂模式的核心思想

工厂模式的核心思想：

> **把对象的创建职责独立出来。**

例如直接创建：

```cpp
auto drone = std::make_unique<DJI>();
```

如果创建过程很复杂：

```cpp
auto drone = std::make_unique<DJI>();

drone->loadConfig();
drone->connect();
drone->initCamera();
drone->initGPS();
```

可以把这些创建和初始化过程集中到一个地方。

---

#### 我的疑问：我直接写一个 createDrone() 不也一样？

是的。

例如：

```cpp
std::unique_ptr<Drone> createDrone()
{
    auto drone = std::make_unique<DJI>();

    drone->loadConfig();
    drone->connect();
    drone->initCamera();

    return drone;
}
```

这实际上已经具有“工厂”的核心思想。

所以：

> **工厂不一定非要有一个 Factory 类。**

如果对象创建非常简单：

```cpp
auto drone = std::make_unique<DJI>();
```

那么直接创建完全没有问题。

---

### 2. 简单工厂模式

#### 核心思想

通过一个统一的工厂函数，根据类型创建不同的对象。

例如：

```cpp
Drone* drone =
    DroneFactory::create("DJI");
```

工厂内部：

```cpp
class DroneFactory
{
public:

    static Drone* create(const std::string& type)
    {
        if (type == "DJI")
            return new DJI();

        if (type == "Military")
            return new MilitaryDrone();

        return nullptr;
    }
};
```

结构：

```text
业务
  ↓
DroneFactory
  ↓
具体对象
```

主要解决：

> **把对象创建逻辑集中管理。**

---

#### 我的疑问：从 DJI 改成 Military 还是要修改代码？

是的。

如果业务代码：

```cpp
DroneFactory::create("DJI");
```

改成：

```cpp
DroneFactory::create("Military");
```

调用处仍然需要修改。

所以：

> **简单工厂不能自动解决“DJI → Military 不修改业务代码”的问题。**

简单工厂主要解决：

> 创建逻辑集中。

而不是：

> 自动决定使用哪个产品。

---

### 3. 工厂方法模式

#### 核心思想

让不同的具体工厂负责创建不同的具体产品。

结构：

```text
             DroneFactory
                  ↑
        +---------+---------+
        |                   |
   DJIFactory        MilitaryFactory
        ↓                   ↓
       DJI            MilitaryDrone
```

抽象工厂：

```cpp
class DroneFactory
{
public:
    virtual Drone* create() = 0;
    virtual ~DroneFactory() = default;
};
```

DJI 工厂：

```cpp
class DJIFactory : public DroneFactory
{
public:

    Drone* create() override
    {
        return new DJI();
    }
};
```

Military 工厂：

```cpp
class MilitaryFactory : public DroneFactory
{
public:

    Drone* create() override
    {
        return new MilitaryDrone();
    }
};
```

---

#### 我的疑问：调用方还是需要知道 DJIFactory，有什么意义？

关键不是：

> **整个程序都不能知道 DJIFactory。**

而是：

> **核心业务代码不应该知道 DJIFactory。**

例如：

```cpp
class Mission
{
public:

    void execute(DroneFactory* factory)
    {
        Drone* drone = factory->create();

        drone->fly();
    }
};
```

`Mission` 只知道：

```text
Drone
DroneFactory
```

不知道：

```text
DJI
MilitaryDrone
DJIFactory
MilitaryFactory
```

外层代码负责选择：

```cpp
DJIFactory factory;

Mission mission;

mission.execute(&factory);
```

如果换成：

```cpp
MilitaryFactory factory;

Mission mission;

mission.execute(&factory);
```

`Mission` 不需要修改。

所以工厂方法的核心：

> **让核心业务依赖抽象，而不是依赖具体实现。**

---

### 4. 注册工厂

#### 为什么需要注册工厂？

传统简单工厂：

```cpp
if (type == "DJI")
    return new DJI();

if (type == "Military")
    return new MilitaryDrone();

if (type == "FPV")
    return new FPVDrone();
```

产品越来越多：

```text
Factory
   ↓
大量 if / else
```

Factory 会越来越大。

---

#### 核心思想

把：

```text
"DJI"      → DJI 创建函数
"Military" → Military 创建函数
"FPV"      → FPV 创建函数
```

保存到一个注册表中。

注册：

```cpp
factory.registerType(
    "DJI",
    [] {
        return std::make_unique<DJI>();
    }
);
```

注册 Military：

```cpp
factory.registerType(
    "Military",
    [] {
        return std::make_unique<MilitaryDrone>();
    }
);
```

创建：

```cpp
auto drone = factory.create("DJI");
```

内部：

```text
create("DJI")
      ↓
   注册表
      ↓
DJI 创建函数
      ↓
     DJI
```

---

#### 注册工厂最大的好处

传统简单工厂增加产品：

```text
增加 SimulationDrone
        ↓
修改 Factory
        ↓
增加 if
```

注册工厂：

```cpp
factory.registerType(
    "Simulation",
    [] {
        return std::make_unique<SimulationDrone>();
    }
);
```

不需要不断修改 Factory 内部的 `if / else`。

所以：

> **注册工厂把“类型 → 创建函数”的关系放到注册表中，使创建系统更容易扩展。**

---

### 5. 简单工厂、工厂方法、注册工厂对比

|          | 简单工厂        | 工厂方法               | 注册工厂         |
| -------- | --------------- | ---------------------- | ---------------- |
| 核心思想 | 集中创建        | 多态创建               | 注册创建         |
| 典型形式 | `create("DJI")` | `DJIFactory::create()` | `registerType()` |
| Factory  | 一个            | 多个具体 Factory       | 一个注册表       |
| 新增产品 | 修改 Factory    | 增加 Factory           | 增加注册项       |
| 主要解决 | 创建逻辑集中    | 业务与具体创建者解耦   | 创建系统可扩展   |

---

### 6. 最终快速记忆

```text

```

---

#### 简单工厂

> **解决“怎么集中创建”。**

```text
Factory::create("DJI")
        ↓
       DJI
```

---

#### 工厂方法

> **解决“业务如何与具体创建者解耦”。**

```text
业务
 ↓
抽象 Factory
 ↓
具体 Factory
 ↓
具体 Product
```

---

#### 注册工厂

> **解决“产品越来越多时，创建系统如何扩展”。**

```text
类型
 ↓
注册表
 ↓
创建函数
 ↓
具体对象
```

---

> 