# 大模型部署基础知识速记

> 面向 vLLM / vLLM-Ascend / Atlas A2 等大模型推理场景。

------

# 一、模型文件格式

模型文件主要解决：

> **“模型权重以什么形式保存？”**

常见格式：

| 格式           | 特点                        | 常见场景           |
| -------------- | --------------------------- | ------------------ |
| `.safetensors` | 安全、高效、只保存 Tensor   | Hugging Face、vLLM |
| `.bin`         | PyTorch 传统权重格式        | 老模型             |
| `.gguf`        | Tensor + Metadata，适合量化 | llama.cpp、Ollama  |

`safetensors` 是目前主流的大模型权重格式之一；GGUF 是面向 GGML/llama.cpp 等推理器优化的格式。

### Sharded 模型

大模型通常会拆成：

```text
model-00001-of-00008.safetensors
model-00002-of-00008.safetensors
...
model-00008-of-00008.safetensors

model.safetensors.index.json
```

**Shard 只是文件拆分，不代表模型被拆成多个独立模型。**

------

# 二、模型精度

常见数据类型：

```text
FP32 → 32 bit → 4 Byte
FP16 → 16 bit → 2 Byte
BF16 → 16 bit → 2 Byte
FP8  → 8 bit  → 1 Byte
INT8 → 8 bit  → 1 Byte
INT4 → 4 bit  → 0.5 Byte
```

### FP16 vs BF16

两者都是：

```text
16 bit = 2 Byte
```

但：

```text
FP16：指数位少、精度较高、动态范围较小
BF16：指数位多、动态范围接近 FP32、尾数精度较低
```

大模型训练/推理中 BF16 很常见。

> **硬件支持某种精度 ≠ 任意模型/算子/框架都能直接使用该精度。**

------

# 三、模型显存粗略估算

最简单：

```text
权重显存 ≈ 参数量 × 每参数字节数
```

例如 100B 参数：

```text
BF16：
100B × 2 Byte ≈ 200 GB

INT8：
100B × 1 Byte ≈ 100 GB

INT4：
100B × 0.5 Byte ≈ 50 GB
```

但实际推理显存还包括：

```text
模型权重
+
KV Cache
+
Activation
+
Workspace
+
通信 Buffer
+
Runtime 开销
```

所以：

> **“参数量 × 字节数”只能估算权重，不能直接等于实际显存需求。**

------

# 四、量化

量化的核心：

> **用更低 bit 的数据表示模型参数/计算数据，从而降低显存和计算/带宽开销。**

常见：

```text
INT8
INT4
AWQ
GPTQ
GGUF Quantization
W8A8
W4A8
W4A8C8
```

### AWQ / GPTQ / GGUF / msModelSlim

不要混为一谈：

```text
AWQ / GPTQ
→ 量化方法/量化模型生态

GGUF
→ 模型文件格式 + llama.cpp 生态

msModelSlim
→ 昇腾模型量化/压缩工具
```

msModelSlim 可用于将模型量化为昇腾推理所需的量化形式，并配合 vLLM-Ascend / MindIE 使用。

------

# 五、MoE 与 Expert

MoE：

> **Mixture of Experts，混合专家模型。**

一个 MoE 模型可以理解成：

```text
模型
│
├── Embedding
├── Attention
├── Router
├── Expert 0
├── Expert 1
├── Expert 2
├── ...
└── Expert N
```

Expert 本质上是模型中的一组 MLP/FFN 子网络。

Router：

```text
Token
 ↓
Router
 ↓
选择 Top-K Expert
 ↓
Expert 计算
```

因此：

> **Expert 是模型权重的一部分，不是一个独立完整模型。**

MoE 的特点：

```text
Total Params
     ↓
非常大

Active Params
     ↓
每个 Token 只激活部分 Expert
```

所以：

```text
总参数量 ≫ 每个 Token 实际参与计算的参数量
```

------

# 六、TP / DP / EP

这是模型的**并行部署方式**。

## 1. TP：Tensor Parallelism

> **一次计算由多少张卡共同完成。**

```text
TP=8

一个计算实例
     ↓
8张 GPU/NPU
共同计算
```

记忆：

> **TP = 切计算**

TP 不等于“一个服务器多少张卡”，但实际部署通常希望 TP 尽量适配单机高速互联拓扑。

------

## 2. DP：Data Parallelism

> **建立多个独立执行实例，处理不同请求。**

例如：

```text
TP=8
DP=4

          模型服务
             │
     ┌───────┼───────┐
     ↓       ↓       ↓       ↓
    DP0     DP1     DP2     DP3
     │       │       │       │
    TP8     TP8     TP8     TP8
```

设备数：

```text
GPU数量 = TP × DP
```

例如：

```text
8 × 4 = 32
```

DP 主要用于：

- 提高吞吐
- 增加并行请求处理能力
- 每个 DP rank 有独立 KV Cache

> **DP 不等于服务器数量。**

------

## 3. EP：Expert Parallelism

专门针对 MoE。

> **把 Expert 分布到多个 GPU/NPU。**

启用 EP 后，vLLM 当前实现中：

```text
EP_SIZE = TP_SIZE × DP_SIZE
```

例如：

```text
TP=8
DP=4

EP=32
```

Expert：

```text
32张卡
   ↓
EP=32
   ↓
Expert 分布到32张卡
```

vLLM 文档明确说明，启用 EP 后，Expert 层使用 `TP × DP` 大小的 EP group；Attention 则在各 DP group 内使用 TP。

------

# 七、TP / DP / EP 的关系

假设：

```text
4台服务器
每台8卡
总共32卡
```

### 方案一

```text
TP=8
DP=4
EP=32
```

拓扑可以是：

```text
Server0 → 8卡 → DP0 → TP8
Server1 → 8卡 → DP1 → TP8
Server2 → 8卡 → DP2 → TP8
Server3 → 8卡 → DP3 → TP8
```

Expert：

```text
32卡
 ↓
EP=32
```

------

### 方案二

```text
TP=16
DP=2
EP=32
```

因为：

```text
16 × 2 = 32
```

但：

```text
DP0
├── Server0 8卡
└── Server1 8卡
       ↓
     TP16

DP1
├── Server2 8卡
└── Server3 8卡
       ↓
     TP16
```

因此 TP=16 必然跨两台服务器。

这意味着：

> **TP 越大，不一定越好；要考虑跨服务器通信。**

对于“4×8卡”的拓扑，`TP=8` 天然更贴合单机 8 卡的情况。vLLM 的并行部署指南也建议单机多卡优先考虑 TP；跨节点时需要结合节点拓扑选择并行策略。

------

# 八、MoE 显存不能简单用“总参数 ÷ DP”

这是理解大模型部署的关键。

MoE：

```text
模型
│
├── Dense / Non-Expert
│
└── Expert
```

EP：

```text
Dense部分
→ 按 TP/DP 等方式分布

Expert部分
→ 按 EP 分布
```

所以不能简单认为：

```text
700B / DP=4
```

就是每个实例承担 175B。

应该分别计算：

```text
Dense 权重
+
Expert 权重
+
KV Cache
+
Activation
+
Runtime
+
通信 Buffer
```

再结合 TP/DP/EP 的实际分片方式计算每张卡的显存。

------

# 九、W4A8C8

`W4A8C8`：

```text
W4 → Weight       4 bit
A8 → Activation   8 bit
C8 → KV Cache     8 bit
```

即：

```text
              W4A8C8
                 │
      ┌──────────┼──────────┐
      ↓          ↓          ↓
    Weight    Activation  KV Cache
      ↓          ↓          ↓
    INT4       INT8       INT8
```

------

## 1. W4

> **权重使用 4 bit。**

主要影响：

```text
模型权重文件大小
模型权重显存
```

例如：

```text
100B参数

BF16 ≈ 200GB
INT8 ≈ 100GB
INT4 ≈ 50GB
```

实际会有 Scale、Metadata、未量化层等额外开销。

------

## 2. A8

> **Activation 使用 8 bit。**

主要影响：

```text
Activation 数据
INT8 计算
数据搬运
计算效率
临时 Activation 显存
```

**A8 不会让模型权重文件变小。**

------

## 3. C8

> **KV Cache 使用 8 bit。**

KV Cache：

```text
K + V
 ↓
保存历史 Token 信息
```

上下文越长：

```text
10K
 ↓
40K
 ↓
128K
 ↓
200K
```

KV Cache 越大。

因此 C8 的核心价值：

> **降低长上下文推理时的 KV Cache 显存。**

------

# 十、W4 / A8 / C8 最简单的区别

```text
W4 → 给模型权重瘦身
A8 → 让计算使用 INT8
C8 → 给 KV Cache 瘦身
```

所以：

| 配置 | 主要作用                                 |
| ---- | ---------------------------------------- |
| W4   | 降低权重显存/存储                        |
| A8   | 降低 Activation 数据精度、优化 INT8 计算 |
| C8   | 降低 KV Cache 显存                       |

特别是长上下文：

> **W4 解决“模型权重太大”，C8 解决“KV Cache 太大”。**

------

# 十一、MTP / DSpark

它们和 TP/DP/EP 不属于同一层。

```text
TP / DP / EP
→ 模型如何分布到多张卡

MTP / DSpark
→ Token 如何更快生成
```

属于：

> **Speculative Decoding（投机解码）**

------

## 1. MTP

MTP：

> Multi-Token Prediction

普通生成：

```text
模型 → Token1
模型 → Token2
模型 → Token3
```

MTP：

```text
模型
 ↓
MTP
 ↓
预测多个 Token
 ↓
Target Model 验证
 ↓
一次接受多个 Token
```

可以理解成：

> **模型自己提前猜多个 Token，再让目标模型验证。**

------

## 2. DSpark

DSpark：

```text
Target Model
     ↓
DSpark Drafter
     ↓
预测多个 Token
     ↓
Target Model 验证
```

区别：

```text
MTP
→ 使用模型自身的 MTP 能力

DSpark
→ 使用专门训练的 Draft Model
```

------

# 十二、完整知识框架

把这些概念放在一起：

```text
                    大模型推理
                         │
       ┌─────────────────┼─────────────────┐
       ↓                 ↓                 ↓
    模型格式            模型量化          推理并行
       │                 │                 │
 safetensors          W4A8C8          TP / DP / EP
 GGUF                  │
 .bin              ┌───┼───┐
                    ↓   ↓   ↓
                   W4  A8  C8
                    │   │   │
                    ↓   ↓   ↓
                  权重 激活 KV Cache
                                          │
                                          ↓
                                    MoE / Expert
                                          │
                                          ↓
                                    EP 分布 Expert
                         │
                         ↓
                    解码加速
                         │
                    ┌────┴────┐
                    ↓         ↓
                   MTP      DSpark
```

------

# 十三、最终速记表

| 概念        | 一句话理解                          |
| ----------- | ----------------------------------- |
| SafeTensors | 主流安全的 Tensor 权重格式          |
| GGUF        | llama.cpp/Ollama 常用的量化模型格式 |
| FP16/BF16   | 16 bit 浮点精度                     |
| INT8/INT4   | 8/4 bit 整数量化                    |
| MoE         | 多 Expert 的模型结构                |
| Expert      | MoE 中的 MLP/FFN 子网络             |
| TP          | **切计算**                          |
| DP          | **多执行实例**                      |
| EP          | **切 Expert**                       |
| W4          | **权重 4 bit**                      |
| A8          | **Activation 8 bit**                |
| C8          | **KV Cache 8 bit**                  |
| MTP         | 模型自身预测多个 Token              |
| DSpark      | Draft 模型预测多个 Token            |

### 最值得记住的两组公式

```text
GPU/NPU 数量 ≈ TP × DP
```

启用 vLLM EP 时：

```text
EP = TP × DP
```

例如：

```text
4台 × 8卡 = 32卡

TP=8
DP=4
EP=32
```

对应：

```text
TP → 一个计算实例使用8卡
DP → 4个并行执行实例
EP → 32卡共同分摊MoE Expert
W4 → 权重压成4bit
A8 → Activation使用8bit
C8 → KV Cache使用8bit
MTP/DSpark → 加速Token生成
```

> **一句话总纲：**
>
> **TP/DP/EP 解决“模型怎么分到多张卡”；W4/A8/C8 解决“不同数据用多少 bit”；MTP/DSpark 解决“Token 怎么更快生成”。**